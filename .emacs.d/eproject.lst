;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("emacs_test" "d:/Developer/Android/projects/emacs_test")
	("eproject" "d:/BIN/TOOLS/emacs/.emacs.d/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top + -8)
	(left + -8)
	(width . 224)
	(height . 36)))

